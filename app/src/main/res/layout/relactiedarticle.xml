<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    xmlns:fresco="http://schemas.android.com/apk/res-auto"
    android:layout_height="wrap_content"
    >


    <com.facebook.drawee.view.SimpleDraweeView
        android:id="@id/relactied_articl_img"
        android:layout_width="match_parent"
        android:layout_height="180dp"
        android:layout_gravity="center"
        fresco:placeholderImage="@drawable/llist_img_default_avatar"
        fresco:placeholderImageScaleType="fitCenter"
        fresco:fadeDuration="1000"
        />
    <TextView
        android:id="@id/relactied_articl_text"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_marginTop="60dp"
        android:layout_gravity="center"
        android:background="@drawable/relactied_articl_text_bg"
        android:padding="5dp"
        android:maxLines="2"
        android:ellipsize="end"
        android:gravity="bottom"
        android:text="tiasdgfadf"
        android:textColor="#fff"
        />

</FrameLayout>